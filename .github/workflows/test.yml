name: Test Workflow
on:
  workflow_dispatch:
    inputs:
      command:
        description: 'Command to run'
        required: false
        default: 'echo hello'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run input
        run: echo "${{ github.event.inputs.command }}"
